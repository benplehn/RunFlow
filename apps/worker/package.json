{
  "name": "@runflow/worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint --ext .ts src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@runflow/config": "workspace:*",
    "@runflow/db": "workspace:*",
    "@runflow/domain": "workspace:*",
    "@runflow/schemas": "workspace:*",
    "@runflow/telemetry": "workspace:*",
    "bullmq": "^5.26.2",
    "dotenv": "^16.4.5",
    "ioredis": "^5.4.1",
    "pino": "^9.14.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
