{
  "name": "@runflow/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"Starting API placeholder...\"",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint --ext .ts src",
    "test": "vitest run --passWithNoTests"
  }
}
