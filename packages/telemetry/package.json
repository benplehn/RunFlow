{
  "name": "@runflow/telemetry",
  "version": "0.0.0",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint --ext .ts src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "pino": "^9.14.0"
  },
  "devDependencies": {
    "pino-pretty": "^11.3.0"
  }
}
